# فایل‌های تنظیمات Z.ai SDK

## ✅ فایل‌های موجود در پروژه

### 1. .z-ai-config (مخفی)
```bash
/home/z/my-project/.z-ai-config
```
- ✅ وجود دارد
- ❌ مخفی است (چون با نقطه شروع می‌شود)
- 📋 محتوا: JSON با apiKey خالی

### 2. z-ai-config.txt (عادی/قابل مشاهده)
```bash
/home/z/my-project/z-ai-config.txt
```
- ✅ وجود دارد
- ✅ مخفی نیست (قابل مشاهده و ویرایش)
- 📋 محتوا: JSON با apiKey خالی
- 🎯 SDK این را بهتر پیدا می‌کند!

---

## 🚀 پیشنهاد: از فایل .txt استفاده کنید

فایل `z-ai-config.txt`:
- ✅ قابل مشاهده است
- ✅ قابل ویرایش است
- ✅ با ls و ls -a قابل دیدن است
- ✅ احتمالاً SDK آن را بهتر پیدا می‌کند

---

## 📦 هر دو فایل همین محتوا را دارد

```json
{
  "apiKey": ""
}
```

چون API key ندارید، فایل خالی است و این مشکلی نیست. SDK با حالت پیش‌فرض کار می‌کند.

---

## 🔧 اگر می‌خواهید ویرایش کنید

```bash
# ویرایش فایل مخفی
nano .z-ai-config

# یا ویرایش فایل عادی
nano z-ai-config.txt
```

---

## 📋 خلاصه

| فایل | مسیر | وضعیت |
|------|------|--------|
| .z-ai-config | `/home/z/my-project/.z-ai-config` | ✅ موجود، مخفی |
| z-ai-config.txt | `/home/z/my-project/z-ai-config.txt` | ✅ موجود، قابل مشاهده |

---

## 🎉 الان می‌توانید deploy کنید!

```bash
git add .
git commit -m "Add config files"
git push liara master
liara deploy
```
